<Page x:Class="PCGManager.Views.editLocalGameFrameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PCGManager.Views"
      xmlns:vm="clr-namespace:PCGManager.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditView"
      Background="Black"
      >
    <Page.DataContext>
        <vm:editLocalGameFrameViewModel/>
    </Page.DataContext>
    <Grid>
        <StackPanel>
        <TextBlock Text="Edit game" Foreground="White" x:Name="TestBlock1"></TextBlock>

            <TextBlock Text="{Binding CurGameProp.name}" Foreground="White" x:Name="TestBlock"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
